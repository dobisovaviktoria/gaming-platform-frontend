# Tic Tac Toe
This is the frontend for the llm tic-tac-toe implementation,
it is in here for now because we have not made a separate repository for this.

This should run our of the box after running `npm install` in this directory.
To run it, use the normal `npm run dev`